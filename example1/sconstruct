# Works for both Python2 and Python3
# Allows us to debug the main script in sconstruct.py from within Visual Studio code
# shouldn't be needed after the next release of scons
exec(compile(open('sconstruct.py').read(), 'sconstruct.py', 'exec'))
